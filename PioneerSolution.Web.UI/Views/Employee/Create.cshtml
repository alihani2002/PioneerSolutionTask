@model PioneerSolution.Services.DTOs.AddEmployeeViewModel

@{
    ViewData["Title"] = "Add Employee";
}

<div class="container mt-4">
    <h2>Add New Employee</h2>
    <p class="text-muted">Fill out the employee details and all custom properties below.</p>

    <div class="card shadow-sm mt-3">
        <div class="card-body">
            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()

                @* ===== Mandatory Employee Fields ===== *@
                <fieldset class="mb-4">
                    <legend class="fw-bold border-bottom pb-2">Basic Information</legend>

                    <div class="mb-3">
                        <label asp-for="Code" class="form-label fw-bold">Employee Code <span class="text-danger">*</span></label>
                        <input asp-for="Code" class="form-control" placeholder="e.g., EMP001" required />
                        <span asp-validation-for="Code" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-bold">Employee Name <span class="text-danger">*</span></label>
                        <input asp-for="Name" class="form-control" placeholder="e.g., John Doe" required />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </fieldset>

                @* ===== Dynamic Property Fields ===== *@
                @if (Model.PropertyDefinitions.Any())
                {
                    <fieldset class="mb-4">
                        <legend class="fw-bold border-bottom pb-2">Custom Properties</legend>

                        @foreach (var prop in Model.PropertyDefinitions)
                        {
                            <div class="mb-3">
                                <label class="form-label fw-bold">
                                    @prop.Name
                                    @if (prop.IsRequired)
                                    {
                                        <span class="text-danger">*</span>
                                    }
                                </label>

                                @switch (prop.Type.ToLower())
                                {
                                    case "string":
                                        <input type="text" class="form-control"
                                               name="PropertyValues[@prop.Id]"
                                               @(prop.IsRequired ? "required" : "") />
                                        break;

                                    case "integer":
                                        <input type="number" class="form-control"
                                               name="PropertyValues[@prop.Id]"
                                               @(prop.IsRequired ? "required" : "") />
                                        break;

                                    case "date":
                                        <input type="date" class="form-control"
                                               name="PropertyValues[@prop.Id]"
                                               @(prop.IsRequired ? "required" : "") />
                                        break;

                                    case "dropdown":
                                        <select class="form-select"
                                                name="PropertyValues[@prop.Id]"
                                                @(prop.IsRequired ? "required" : "")>
                                            <option value="">-- Select --</option>
                                            @if (!string.IsNullOrEmpty(prop.DropdownExpectedValues))
                                            {
                                                @foreach (var option in prop.DropdownExpectedValues.Split(',', System.StringSplitOptions.TrimEntries))
                                                {
                                                    <option value="@option">@option</option>
                                                }
                                            }
                                        </select>
                                        break;

                                    default:
                                        <input type="text" class="form-control"
                                               name="PropertyValues[@prop.Id]" />
                                        break;
                                }
                            </div>
                        }
                    </fieldset>
                }
                else
                {
                    <div class="alert alert-warning">
                        No custom properties defined yet.
                        <a asp-controller="Property" asp-action="Create">Create one here</a>.
                    </div>
                }

                <div class="mt-4">
                    <button type="submit" class="btn btn-success px-4">Save Employee</button>
                    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>
